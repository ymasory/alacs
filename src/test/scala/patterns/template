package com.github.alacs

import org.scalatest.FunSuite

class AlacsPatternXXXTests extends AlacsPatternSuite {

  override val id = XXX

  test("positive example") {
    val bugs: List[Bug] = run("DivByZeroHex")
    check(bugs)
  }

  test("negative example") {
    expect(Nil) { run("DivByOne") }
  }
}
